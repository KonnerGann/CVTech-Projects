function cConverter() 
{
    //variables//
    let ogTemp = document.getElementById("temp");
    let result = documnet.getElementById("result");
    let output = null
    //If Function//
    if (output = null) {
        output = (ogTemp - 32) * 5 / 9
    }
    result.textContent = `${ogTemp}F is ${output}C`;
};
